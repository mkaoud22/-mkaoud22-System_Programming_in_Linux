# echo – A Simple Unix `echo` Utility in C

This program is a basic implementation of the Unix `echo` command written in C.  
It prints the given input string(s) to standard output (stdout), mimicking the behavior of the built-in `echo` command.

## 📁 File

- `echo.c`: Source code for the `echo` command.

## 🔧 How to Compile

Use `gcc` to compile the program:

```bash
gcc -o echo echo.c

or 

you can use the make command to generate the executable file.

